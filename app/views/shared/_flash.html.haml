#flash
  - flash.each do |name, msg|
    - if msg.is_a?(String)
      = content_tag :div, 'data-closable' => '', class: "callout small #{name}" do
        %button.close-button{'aria-label' => 'dismiss alert', 'data-close' => '', :type => 'button'}
          %span{'aria-hidden' => 'true'} ×
        = msg